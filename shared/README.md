# shared

Cross-cutting type/schema definitions shared between frontend and backend
(e.g. `Node`, `Branch`, `StepEvent`, `EngineConfig` per the build plan).

Empty for now — populated as the data model lands in M1/M2.
