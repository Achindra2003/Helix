"""Shared prompt library (F4) — workspace-scoped, saveable, searchable, reusable.

A team's hard-won prompts are an asset: the library lets anyone in a workspace
save a "winning" prompt and reuse it to drive a turn in any conversation. This is
the last missing *core* engine feature (the deep-reasoning engine is the bonus).
"""
