fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
pydantic==2.10.4
# 2.7.1 was unsatisfiable alongside requirements-engine.txt: langchain-community
# 0.4.2 requires pydantic-settings>=2.10.1. It only appeared to work because the
# two requirement files were installed as separate pip commands, which let the
# second silently upgrade the pin — a clean single-command install (as in the
# Docker build) fails with ResolutionImpossible. 2.14.2 is what every existing
# environment has actually been running, and what the 261-test suite passes on.
pydantic-settings==2.14.2
email-validator==2.2.0
pyjwt==2.10.1
bcrypt==4.2.1
httpx==0.28.1
cryptography==49.0.0
python-dotenv==1.0.1
python-multipart==0.0.32
pypdf==6.14.2
# LLM observability: GenAI spans, exported only when an OTLP endpoint is
# configured (e.g. self-hosted Langfuse); no-op otherwise (api/telemetry.py).
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
